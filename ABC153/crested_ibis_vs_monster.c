#include <stdio.h>

int main(void)
{
    int H, N;
    int A[1000]; // number of magic
    int B[1000]; // number of spent magic points
    int i;
    int magic_count[1000]
    scanf("%d %d", &H, &N);
    for (i = 0; i < N; i++)
    {
        scanf("%d %d", &A[i], &B[i]);
    }
    for (i = 0; i < N; i++)
    {
        
    }
    


    return (0);
}